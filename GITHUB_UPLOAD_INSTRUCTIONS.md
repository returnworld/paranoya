# Инструкции по загрузке проекта на GitHub

## Подготовка проекта

✅ Создан файл `.gitignore` для исключения конфиденциальных файлов
✅ Файл `.env.example` содержит шаблон переменных окружения
✅ Все необходимые файлы проекта готовы к загрузке

## Шаги для загрузки на GitHub

### 1. Создание репозитория на GitHub

1. Перейдите на [GitHub.com](https://github.com)
2. Войдите в свой аккаунт
3. Нажмите кнопку "New" (или значок "+" в правом верхнем углу → "New repository")
4. Заполните информацию о репозитории:
   - **Repository name**: `paranoya` (или любое другое название)
   - **Description**: `Application for logging visitors with IP geolocation`
   - **Visibility**: выберите Public или Private
   - **⚠️ НЕ ставьте галочку** "Add a README file", "Add .gitignore", "Choose a license" (у нас уже есть эти файлы)
5. Нажмите "Create repository"

### 2. Инициализация Git в локальном проекте

Откройте терминал в папке проекта и выполните команды:

```bash
# Инициализация Git репозитория
git init

# Добавление всех файлов в индекс
git add .

# Создание первого коммита
git commit -m "Initial commit: Visitor logging application"

# Добавление удаленного репозитория (замените YOUR_USERNAME на ваше имя пользователя GitHub)
git remote add origin https://github.com/YOUR_USERNAME/paranoya.git

# Отправка кода на GitHub
git branch -M main
git push -u origin main
```

### 3. Альтернативный способ через GitHub CLI

Если у вас установлен GitHub CLI:

```bash
# Создание репозитория через CLI
gh repo create paranoya --public --source=. --push

# Или с описанием
gh repo create paranoya --public --description="Application for logging visitors with IP geolocation" --source=. --push
```

### 4. Проверка загрузки

1. Перейдите на страницу вашего репозитория на GitHub
2. Убедитесь, что все файлы загружены корректно
3. Проверьте, что `.env` файл отсутствует (он должен быть в .gitignore)

## Структура проекта на GitHub

После загрузки ваш репозиторий будет содержать:

```
paranoya/
├── .env.example          # Шаблон переменных окружения
├── .gitignore           # Исключения для Git
├── README.md            # Документация проекта
├── package.json         # Зависимости и скрипты
├── package-lock.json    # Заблокированные версии зависимостей
├── server.js            # Основной файл сервера
├── models/              # Модели данных
│   └── Visit.js
└── public/              # Статические файлы
    ├── index.html
    ├── sketch.js
    ├── stats.html
    ├── style.css
    ├── gol.vert
    └── gol.frag
```

## Следующие шаги

1. **Настройте README.md** - добавьте подробное описание проекта, инструкции по установке и использованию
2. **Создайте релиз** - когда проект будет готов, создайте тег версии
3. **Настройте GitHub Pages** (если нужно) - для хостинга статических файлов
4. **Добавьте лицензию** - рекомендуется MIT License

## Безопасность

⚠️ **Важно**: Файл `.env` никогда не должен попадать в репозиторий. Он содержит конфиденциальную информацию (пароли, API ключи). Вместо этого используйте `.env.example` как шаблон.

## Команды для обновления проекта

После внесения изменений в код:

```bash
# Добавление изменений
git add .

# Создание коммита с описанием изменений
git commit -m "Description of your changes"

# Отправка на GitHub
git push
```

---

**Проект готов к загрузке на GitHub!** 🚀